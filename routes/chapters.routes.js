const router = require("express").Router();

// // GET "/books/:bookId/chapters" => Mostrar listado de todos los capítulos
// // GET "/chapters" => Mostrar listado de todos los capítulos
// router.get("/");

module.exports = router;
